# Project 05 - Backup & Restore Configuration

LAB_ROOT="/opt/devopsshack/backup-lab"

SOURCE_DIR="$LAB_ROOT/test-data"
BACKUP_DIR="$LAB_ROOT/backups"
STATE_DIR="$LAB_ROOT/state"
LOG_DIR="$LAB_ROOT/logs"
RESTORE_DIR="$LAB_ROOT/restore-test"

RETENTION_DAYS=7

RPO_MINUTES=60
RTO_MINUTES=30

COMPRESSION="gzip"
CHECKSUM_ALGORITHM="sha256"

FULL_BACKUP_DAYS=7
SNAPSHOT_FILE="$STATE_DIR/test-data.snar"
MIN_FREE_MB=100

RETENTION_CHAINS=2

CHAIN_STATE_FILE="$STATE_DIR/chain_id"

LAST_FULL_FILE="$STATE_DIR/last_full_epoch"

DB_BACKUP_ENABLED=false
DB_TYPE=""
DB_NAME=""
DB_USER=""
DB_HOST="localhost"
DB_PORT=""
DB_BACKUP_DIR="$LAB_ROOT/backups/db"
