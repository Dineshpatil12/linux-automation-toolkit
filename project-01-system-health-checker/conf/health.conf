# CPU thresholds (%)
CPU_WARN=75
CPU_CRIT=90

# Memory thresholds (%)
MEM_WARN=80
MEM_CRIT=92

# Swap thresholds (%)
SWAP_WARN=20
SWAP_CRIT=50

# Disk thresholds (%)
DISK_WARN=80
DISK_CRIT=90

# Inode thresholds (%)
INODE_WARN=80
INODE_CRIT=90

# Load average per CPU core
LOAD_WARN=1.5
LOAD_CRIT=2.5

# Number of top processes to display
TOP_PROCESSES=5
